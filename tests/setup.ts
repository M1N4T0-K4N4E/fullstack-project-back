import { beforeAll, afterAll, afterEach } from 'vitest';

// Global test setup
beforeAll(() => {
  // Initialize any global test dependencies
});

afterEach(() => {
  // Clean up after each test
});

afterAll(() => {
  // Global teardown
});

// Optional: Mock environment variables for tests
process.env.NODE_ENV = 'test';
